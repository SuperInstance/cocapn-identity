[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cocapn-identity"
version = "0.1.0"
description = "Fleet agent identity and authentication"
requires-python = ">=3.9"

[tool.hatch.build.targets.wheel]
packages = ["src/cocapn_identity"]
